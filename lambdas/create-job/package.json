{
  "name": "poc-create-job",
  "version": "1.0.0",
  "description": "Creates job and saves Task Token for Step Functions callback",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist *.zip",
    "package": "npm run build && cd dist && zip -r ../create-job.zip .",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/client-lambda": "^3.500.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/aws-lambda": "^8.10.134",
    "typescript": "^5.3.3"
  }
}
